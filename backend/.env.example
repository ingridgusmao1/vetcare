# Server
PORT=4000
NODE_ENV=development

# Database (default values when running PostgreSQL locally)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/vetcare

# JWT
# Generate a strong secret with:  node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=c408f61876e4248fe41b641a220b960512fdd5856dd18b3d03725f4a8f11c6e5aa64e0b7df145cee88900fb855592df22753b3c793bef740de8412136e300610
JWT_EXPIRES_IN=8h

# CORS — comma-separated list of allowed origins
CORS_ORIGIN=http://localhost:5173

# Cookie security: set to 'true' in production over HTTPS, 'false' for local HTTP.
COOKIE_SECURE=false

DB_USER=ingrid
DB_PASSWORD=123
DB_HOST=localhost
DB_PORT=5432
DB_NAME=vetcare